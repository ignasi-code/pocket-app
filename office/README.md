# Office Node

This folder contains the phone-first business runtime layer.

The office node is the private operational environment for one or many small businesses. It runs on Termux, pulls work from cloud queues/inboxes/databases, and acts through AI, APIs, email, and account-specific sessions.

The public web surface should stay static-first and edge-hosted. The office node should be needed for operations, not normal page loads.
